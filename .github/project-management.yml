# Labels and milestones defined here have been applied to the repository.
# They are managed directly on GitHub — no workflow needed.
# To re-apply or update, use:
#   gh label create <name> --color <hex> --repo ricardogr07/reposage --force
#   gh api repos/ricardogr07/reposage/milestones --method POST -f title=<title> -f description=<desc>
